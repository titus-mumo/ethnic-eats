version: '3.8'

services:
  web:
    build: .
    command: daphne -b 0.0.0.0 -p 8000 ethnic_eats.asgi:application
    volumes:
      - .:/ethnic_eats
    ports:
      - "8000:8000"
    environment:
      DJANGO_SETTINGS_MODULE: ethnic_eats.settings

volumes:
  postgres_data:
